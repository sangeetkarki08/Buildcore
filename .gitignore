# NOTE: js/supabase-config.js is intentionally committed so GitHub Pages
# can serve a configured app. It only holds the publishable (anon) key,
# which is browser-safe — Row Level Security protects the data.
.env
.env.local

# Supabase CLI
.branches
.temp
supabase/.temp

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*~

# Logs
*.log
npm-debug.log*
