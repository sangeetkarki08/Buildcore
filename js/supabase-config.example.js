/* Copy this file to js/supabase-config.js and fill in your project's
 * values. The anon key is safe to ship to the browser — Row Level
 * Security in supabase/schema.sql is what actually protects the data.
 *
 * js/supabase-config.js is git-ignored so each deployment can set its
 * own project without committing credentials.
 *
 * Find these under: Supabase Dashboard -> Project Settings -> API
 */
window.BUILDCORE_SUPABASE = {
  url: 'https://YOUR-PROJECT.supabase.co',
  anonKey: 'YOUR-ANON-KEY'
};
